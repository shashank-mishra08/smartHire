import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "SmartHire AI — Intelligent Interview Scheduling",
  description:
    "AI-powered interview scheduling agent that naturally converses with candidates, checks calendar availability, and automates the entire hiring workflow.",
  keywords: ["interview scheduling", "AI", "hiring", "recruitment", "automation"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
